# ==========================================
# SGE (Sakin Go Edition) - Environment Config
# ==========================================

# --- Infrastructure ---
# NATS JetStream
NATS_URL=nats://localhost:4222
NATS_USER=admin
NATS_PASSWORD=sakin123

# Redis (Cache, State)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# PostgreSQL (OLTP: Users, Rules, Alerts)
POSTGRES_ADDR=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=sakin123
POSTGRES_DB=sge_db

# ClickHouse (OLAP: Events, Flows, Logs)
CLICKHOUSE_ADDR=localhost
CLICKHOUSE_DB=default
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# --- Services Configuration ---

# SGE Ingest
INGEST_HTTP_PORT=8081
INGEST_SYSLOG_PORT=5140

# SGE Enrichment
# Get free keys from: https://www.abuseipdb.com/ & https://otx.alienvault.com/
ABUSEIPDB_KEY=your_abuseipdb_key_here
OTX_KEY=your_otx_key_here
MAXMIND_DB_PATH=./GeoLite2-City.mmdb

# SGE Panel API
PANEL_PORT=:8080
JWT_SECRET=super-secret-sakin-key-change-me

# SGE Agent (Local Testing)
AGENT_ID=local-dev-agent-01
SERVER_URL=tls://localhost:4222
CERT_FILE=./certs/client-cert.pem
KEY_FILE=./certs/client-key.pem
CA_FILE=./certs/ca-cert.pem

# --- Feature Flags ---
ENABLE_DEBUG=true
